import { OnInit, ElementRef, Injector } from '@angular/core';
export declare class DatagridHeaderCheckboxComponent implements OnInit {
    private injector;
    checked: boolean;
    disabled: boolean;
    indeterminate: boolean;
    chk: ElementRef;
    private dfs;
    private dg;
    constructor(injector: Injector);
    ngOnInit(): void;
    updateStatus(checked: boolean): void;
    handleClick(event: MouseEvent): void;
}
