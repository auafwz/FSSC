import { MessageBody } from './messagebody';
import { AppContext } from '../app/app_context';
export declare class MessageManager {
    private curAppContext;
    private constructor();
    /**
     * 获取当前window上的MessageManager对象
     */
    static getMessager(): MessageManager;
    listner(callbackfn: (e: MessageBody) => any): void;
    send(msg: MessageBody): void;
    setAppContext(appContext: AppContext): void;
    getAppContext(): AppContext;
}
