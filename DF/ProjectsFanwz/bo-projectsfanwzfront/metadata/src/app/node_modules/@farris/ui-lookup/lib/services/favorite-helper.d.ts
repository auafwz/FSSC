import { ComponentRef } from '@angular/core';
import { TreeNode } from '@farris/ui-treetable';
import { FavoriteIcon } from '../lookup-displaytype';
import { LookupGridComponent } from '../lookup-grid.component';
export declare class FavoriteHelper {
    private instance;
    private lookupSelectionSer;
    constructor(instance: LookupGridComponent);
    getFavoriteColumns(): any[];
    favoriteColumnFormatter: (v: any, data: any) => "" | FavoriteIcon.yes | FavoriteIcon.no;
    initPersonalInfo(): void;
    private getPersonalConfigKey;
    /**
     * 监听收藏TAB页中相关事件；
     * 数据选中，取消选中，移除收藏，双击事件
     */
    initFavoriteComponentEvent(cmpRef: ComponentRef<any>): void;
    private getFavoritData;
    getFavoritIds(): any[];
    _loadFavoriteData(data?: any): void;
    loadFavoritesData(res?: any): void;
    private loadFavoriteDatatable;
    private bindFavTreetable;
    initFavoriteTreeData(items: any): TreeNode[];
    loadFavoriteForTreeTable(items: any, ftt: any): any;
    updateFavoritesStatus(data: any[]): void;
    private _updateFavorites;
    private listenPersonalConfigHandler;
}
